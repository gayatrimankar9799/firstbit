//Write a C program to find the square and cube of a given number.

void main()
{
	int side=5,square,cube;
	square=side*side;
	printf("square %d is %d ",side,square);
	cube=side*side*side;
	printf("\n cube %d is %d ",side,cube);
}