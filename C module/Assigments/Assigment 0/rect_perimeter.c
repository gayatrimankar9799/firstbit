//Write a C program to input the length and width of a rectangle and find its perimeter.

void main()
{
	int length=20,width=30,rect,perimeter;
	rect=length*width;
	printf("Area of rectangle %d",rect);
	perimeter=2*(length+width);
	printf("\n Area of perimeter recttangle %d",perimeter);
}